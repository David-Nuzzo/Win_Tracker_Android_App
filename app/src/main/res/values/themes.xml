<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.WinTracker" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/green_lighter</item> <!-- Used for status bar at top of phone. -->
        <item name="android:windowBackground">@color/black</item> <!-- Used for the app background -->
        <item name="colorPrimary">@color/green_darker</item> <!-- Used for the button colour -->
    </style>
</resources>